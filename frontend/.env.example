# Copy to .env and fill in values from the Supabase dashboard >
# Project Settings > API. These are the public URL + anon key — safe
# to ship to the browser (see lib/supabaseClient.ts for why).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
